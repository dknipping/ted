server:
    port: ${PORT:9090}
spring:
    application:
        name: admin
    boot:
        admin:
            url: http://localhost:${server.port}
    cloud:
        config:
            enabled: false
management:
    contextPath: /management