server:
  port: 9000

management:
  context-path: /management
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dknipping/ted/ted-configuration
          basedir: target/config