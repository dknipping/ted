server:
  port: 9000

management:
  context-path: /management
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dknipping/ted-configuration.git
          basedir: target/config